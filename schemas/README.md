# Schemas

Versioned protocol and data contracts live here.

Schemas should be:

- machine-readable;
- backward-compatible where practical;
- explicitly versioned;
- validated in tests.

The first bootstrap schema is `run-record.schema.json`.
